services:
  lylink:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/var/www/html
    ports:
      - "1592:80"
    command: ["php", "-S", "0.0.0.0:80", "-t", "/var/www/html"]